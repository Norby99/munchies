apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: restaurant-mongodb
  namespace: restaurant-service
spec:
  serviceName: restaurant-mongodb
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-mongodb
  template:
    metadata:
      labels:
        app: restaurant-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: restaurant-service
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          volumeMounts:
            - name: restaurant-mongo-data
              mountPath: /data/db
      volumes:
        - name: restaurant-mongo-data
          persistentVolumeClaim:
            claimName: restaurant-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-mongodb
  namespace: restaurant-service
spec:
  selector:
    app: restaurant-mongodb
  ports:
    - port: 27017
      targetPort: 27017
