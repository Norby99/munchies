apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: table-reservation-mongodb
  namespace: table-reservation-service
spec:
  serviceName: table-reservation-mongodb
  replicas: 1
  selector:
    matchLabels:
      app: table-reservation-mongodb
  template:
    metadata:
      labels:
        app: table-reservation-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: table-reservation-service
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          volumeMounts:
            - name: table-reservation-mongo-data
              mountPath: /data/db
      volumes:
        - name: table-reservation-mongo-data
          persistentVolumeClaim:
            claimName: table-reservation-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: table-reservation-mongodb
  namespace: table-reservation-service
spec:
  selector:
    app: table-reservation-mongodb
  ports:
    - port: 27017
      targetPort: 27017
