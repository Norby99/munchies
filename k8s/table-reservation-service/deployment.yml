apiVersion: apps/v1
kind: Deployment
metadata:
  name: table-reservation-service
  namespace: table-reservation-service
  labels:
    app: table-reservation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: table-reservation-service
  template:
    metadata:
      labels:
        app: table-reservation-service
    spec:
      containers:
        - name: table-reservation-service
          image: table-reservation-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: MICRONAUT_ENVIRONMENTS
              value: "k8s,prod"
            - name: MONGODB_URI
              value: "mongodb://table-reservation-mongodb:27017/table-reservation-service"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: table-reservation-service
  namespace: table-reservation-service
spec:
  selector:
    app: table-reservation-service
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort
