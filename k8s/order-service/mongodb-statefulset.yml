apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-mongodb
  namespace: order-service
spec:
  serviceName: order-mongodb
  replicas: 1
  selector:
    matchLabels:
      app: order-mongodb
  template:
    metadata:
      labels:
        app: order-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: order-service
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          volumeMounts:
            - name: order-mongo-data
              mountPath: /data/db
      volumes:
        - name: order-mongo-data
          persistentVolumeClaim:
            claimName: order-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: order-mongodb
  namespace: order-service
spec:
  selector:
    app: order-mongodb
  ports:
    - port: 27017
      targetPort: 27017
