$kind: http-request
description: Placeholder request for deleting the test user created earlier.
url: "{{baseUrl}}/users/{{userId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test('Cleanup delete user returned an expected status', function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 202, 204]);
      });
    language: text/javascript
settings: {}
order: 1000
