$kind: http-request
description: Optional placeholder request for removing seeded data or resetting the environment after the E2E flow.
url: "{{baseUrl}}/cleanup"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "note": "Replace with cleanup payload for your API"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Reset request completed', function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 202, 204]);
      });
    language: text/javascript
settings: {}
order: 2000
